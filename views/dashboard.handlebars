    <h2>Dashboard</h2>

    <form class="form">
      <div>
        <label for="blog-title">Title</label>
        <input type="text" id="blogTitle" name="blog-title" />
      </div>
      <div>
        <label for="blog-text">Text</label>
        <textarea id="blog-text" name="blogText"></textarea>
      </div>
      <button type="submit" class="btn">Submit</button>
    </form>
  </div>

  <div class = "column">
    <h2>About Me</h2>
    {{!-- {{> user-info}} --}}
  </div>

</section>

{{#if blogs.length}}
<section>
  <h2>Your Posts</h2>
    {{#each blogs as |blog|}}
    <div class="card">
      <div class="card-header">
        <h4>{{blog.name}}</h4>
        <p>{{blog.content}}</p>
        <p class="blockquote-footer">Posted on {{format_date created_at}}</p>
        <button class="delete" onclick="deleteBlog({{blog.id}})" id="delete-blog-{{blog.id}}"
      </div>
    </div>
    {{/each}}
    </section>
    {{/if}}
    
<button type="submit" class="btn"><a class="link" href="/dashboard/new">+ New Post</a></button>
<script src='/js/delete-post.js'></script>
    
<!--Script to add a post-->
<script src="../public/js/add-post.js"></script>